# This empty file keeps qmake from picking up the repository-root
# .qmake.conf (which configures the Qt module/plugin build). The preview
# program is a plain application, so it must not inherit that configuration.
